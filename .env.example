# Copy to .env and adjust as needed. app.py reads these via os.environ;
# python-dotenv is not wired up, so either export them in your shell or
# your process manager, or `pip install python-dotenv` and load it yourself.

SECRET_KEY=change-me-to-something-random
PORT=5000
BANK_DB_PATH=bank.db
